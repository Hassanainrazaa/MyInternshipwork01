import 'package:flutter/material.dart';

class TwitterSearch extends StatelessWidget {
  const TwitterSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
