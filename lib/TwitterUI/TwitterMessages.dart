import 'package:flutter/material.dart';

class TwitterMessage extends StatelessWidget {
  const TwitterMessage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
