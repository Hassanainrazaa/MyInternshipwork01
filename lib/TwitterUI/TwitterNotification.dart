import 'package:flutter/material.dart';

class TwitterNotification extends StatelessWidget {
  const TwitterNotification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
